import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  addPetBtn: {
    fontSize: 18,
    paddingTop: 15,
    paddingBottom: 15,
    margin: 15,
    borderRadius: 30,
    color: 'white',
    textAlign: 'center',
    backgroundColor: '#64C962',
  },
});
